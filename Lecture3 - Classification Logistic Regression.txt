Machine Learning Technologies
Classification

Logistic Regression

Donny Hurley

ATU Galway

Donny Hurley Classification 1 / 24



Classification

Now some different problems:

With regression, we assumed we were taking input and producing a quantity: what
number (continuous value) do we think y will be (close to) for a particular input?

Many ML problems have a more limited set of outputs. Often the question is
more like: which group is this input (mostly likely) part of?

These are classification problems.

Our outputs will be restricted to the possible classes for our problem. i.e. for n
samples with K categories, we predict a vector of n values chosen from C1,C2, . . . ,Ck .
Our inputs will stay the same: an n ×m matrix of numbers (for n samples and m
features).

Donny Hurley Classification 2 / 24



Classification

Some problems that can be seen as classification:

Is this email spam? Is this credit card charge fraud? (2 categories)

What (English) word did the user say into the microphone? (≈470k categories)

What day will ATU Galway open next year? (<<365 categories) Or is that
regression?

How many people are in this picture? Who are the people?

Given an RGB colour value, what colour word describes it?

Given weather observations, can you guess what city are we looking at?

Donny Hurley Classification 3 / 24



Modelling

There are many ways to model classification.
All do the same basic job:

1 train on known data
2 then make predictions about correct category for other inputs.

1 model = SomeC l a s s i f i e rMode l ( . . . . )
2 model . f i t ( X t r a i n , y t r a i n )
3 y p r e d i c t e d = model . p r e d i c t ( X t e s t )
4 somehow compare r e su l t s ( y t e s t , y p r e d i c t e d )

(Often use validation to pick the best form of the Classifier, previously used

1 model = L i n e a rR e g r e s s i o n ( . . . . )

it all takes the same basic idea in code)

Donny Hurley Classification 4 / 24



Evaluating Classification Performance

For evaluating Classification algorithms often people just see how accurate the
predictions are on the test set

1 y p r ed = model . p r e d i c t ( X t e s t )
2 from s k l e a r n . me t r i c s impor t a c c u r a c y s c o r e
3 a c c u r a c y s c o r e ( y t e s t , y p r ed )

Will give a score (< 1) which is the fraction of how many predictions were correct.

This stores the y predictions separately (which may be useful later) but the model has
a method itself that can be used if you just want the accuracy score

1 model . s c o r e ( X te s t , y t e s t )

This one line does the predicting and comparing of predictions in the same step.
We will come back to this later, about alternatives.

Donny Hurley Metrics 5 / 24



Binary Classification

Definition

A Binary classification problem has:

Negative Class

Positive Class

y ∈ [0, 1] or y ∈ [−1, 1]

We’ll consider Multi-class problems later. Some models can directly address multi-class
(Näıve Bayes, k-nearest neighbours) while some are inherently binary but can be
”extended” (SVM, Logistic Regression)

Donny Hurley Binary Classification 6 / 24



Categorical Response Variable

Methods for use when the response variable (ŷ) has only two possible outcomes: a
customer buys or does not buy a product, a patient lives or dies, or a candidate
accepts or declines a job offer.

In general, we call the two outcomes of the response variable ”success” and ”failure”
and represent them by 1 and 0. The mean is then the proportion of 1s.

Donny Hurley Logistic Regression 7 / 24



Logistic Regression

To start on classification, do we really need a different algorithm? Let’s see if we can
”tweak” what we know about regression.
Note:

Logistic Regression is a confusing name because we are not trying to predict a
continuous value.

We do predict a continuous value but we then threshold it to take on discrete
(non-continuous) values.

Logistic Regression, in the end, is actually a classification algorithm.

Donny Hurley Logistic Regression 8 / 24



Logistic Regression

Despite its name, Logistic Regression is a classification ML model. Something is either
in category 0 or 1 and the predicted value is between 0 and 1. The predicted value is a
probability

The response variable y is either 0 or 1. By our standard formulae for regression you
will recall it is

ŷ = w0 + w1x1

The estimation of ŷ has a good chance of being < 0 or > 1. We want an estimation
for p between 0 and 1.

Donny Hurley Logistic Regression 9 / 24



Logistic Regression

Additionally we get a plot (only one fea- The picture clearly is not going to be

ture) like very accurate. We would much rather a
picture like

Donny Hurley Logistic Regression 10 / 24



Model for Logistic Regression

Sigmoid function, also called the logistic function, takes any real-valued number and
maps it to a value between 0 and 1. If the curve tends towards +∞, y goes towards 1
and if it tends towards −∞, y goes towards 0

1
f (x) =

1 + e−x

Note: Sigmoid function has regularly
been used in the past for Neural Net-
works, Deep Learning, the main one in
the past
We have the function that gives us the shape we want, how do we relate this to our
ŷ = w0 + w1x?

Donny Hurley Logistic Regression 11 / 24



Model for Logistic Regression

Definition
Logit Function ( )

p
logit(p) = log

1− p

where log is the natural logarithm (probably ln on your calculator, log with base e
(exponential)).

Inverse logit is the sigmoid function and

exp(x) 1
=

1 + exp(x) 1 + exp(−x)

Donny Hurley Logistic Regression 12 / 24



Logistic Regression

We use the sigmoid function
1

1 + exp(−x)

to bind our values between(0 and 1), then (log means natural log)

y
log = w w x + . + n n

1− 0 + 1 1 . . w x .
y

where x describes the features of a datapoint. So

1
fw(x) = ŷ =

1 + e−(w0+w1x1+...+wnxn)

Donny Hurley Logistic Regression 13 / 24



Logistic Regression

Despite its name, Logistic Regression is a classification ML model.

Something is either in category 0 or 1 and the predicted value is between 0 and 1. The
predicted value is a probability

Donny Hurley Logistic Regression 14 / 24



Decision Boundary
We expect our classifier to give us a set of outputs or classes based on probability
when we pass the inputs through a prediction function and returns a probability score
between 0 and 1.
Example

We have 2 classes, cats and dogs (1 - dog , 0 - cats). We basically decide with a
threshold value above which we classify values into Class 1 and of the value goes below
the threshold then we classify it in Class 2.

Chosen the threshold as 0.5: if the
prediction function returned a value of
0.7 then we would classify this obser-
vation as Class 1(DOG). If our predic-
tion returned a value of 0.2 then we
would classify the observation as Class
2(CAT).

We can choose a different boundary to adjust for FP vs FN.
Donny Hurley Logistic Regression 15 / 24



Threshold

The end result of our ML system (for a binary classifier) is either the number 0 or
1.

ML systems will often only output the class it thinks is most likely with this
threshold, i.e. if one side of decision boundary always output 0, if the other
output 1

A lot of ML libraries that deal with Logistic Regression you can retrieve the
“probability” for its result, however, some may just output 0 or 1.

Donny Hurley Logistic Regression 16 / 24



Logistic Regression

Clearly this is a binary classification system, so what happens if we have multiple
categories?

For now we’ll just take the scikit learn method as “magic” in how it deals with
multiple categories. I’ll explain how this works later.

A version of Logistic Regression is very often applied on the last layer of a neural
network so understanding it will help when we get to that later.

Donny Hurley Logistic Regression 17 / 24



Loss Function

To train a logistic regression model, we have to figure out what weights (w) minimise
the cost/loss. We have to choose the cost function

Mean Squared Error performs very poorly for Logistic Regression

1 ∑m ( )2
y (i) − fw (x

(i))
2m

i=1

it would end up being a non-convex function with many local minimum (so difficult to
find the global minimum) instead us{e this Cost function

− log (fw(x)) if y = 1
L(fw(x), y) = − log (1− fw(x)) if y = 0

the loss for one datapoint.

Donny Hurley Logistic Regression 18 / 24



Loss Function (Log-Loss)

This is not differentiable as stated so let’s combine it to

L(fw(x), y) = −y log (fw(x))− (1− y) log (1− fw(x))

Of course we need to do this over all samples. Therefore we use the following as our
Loss Function for logi[stic regression

∑m ( ) ( )]
L(w) = − 1 −y (i) log fw(x

(i)) − (1− y (i)) log 1− fw(x
(i))

m
i=1

The function is convex which greatly simplifies minimisation. We need to find the
optimum parameters w that minimises the loss on the training set. Then we can use
the model to predict new outputs on unseen data.

The algorithm to solve the problem and find the w is the same as others and we’ll,
again, see it later.

Donny Hurley Logistic Regression 19 / 24



Programming

1 from s k l e a r n . l i n e a r mo d e l impor t L o g i s t i c R e g r e s s i o n

It has some options, but we’ll mostly just keep the default

1 p en a l t y : { ’ l 1 ’ , ’ l 2 ’ , ’ e l a s t i c n e t ’ , None } , d e f a u l t=’ l 2 ’

is a hyperparameter for it, the default is good - I’m just pointing it out, I’ll explain
later.

1 max i t e r : i n t , d e f a u l t =100

I’ve mentioned a few times about how the learning is doing things over and over again.
Here we can control the max number of times it will do this process. Sometimes you
might need more.

Donny Hurley Logistic Regression 20 / 24



Programming

Now to talk about the outputs. Like LinearRegression it has

1 model . c o e f
2 model . i n t e r c e p t
3 model . s c o r e ( X te s t , y t e s t ) # G iv e s the ” accu racy ” s c o r e o f the model
4 y p r ed = model . p r e d i c t ( X t e s t )

A new one it has is

1 model . c l a s s e s

This gives you a list of class labels known to the model. e.g. your y’s are

1 y = np . a r r a y ( [ 0 , 1 , 2 , 1 , 1 , 1 , 1 , 1 , 0 , 2 , 1 ] ) #or a pandas s e r i e s e q u i v a l e n t

then model.classes will be [0, 1, 2]. If your y’s are

1 y = np . a r r a y ( [ ” ca t ” , ”dog” , ” p ikachu ” , ”dog” , ”dog” , ”dog” , ” ca t ” , ” p ikachu ” ] )

then model.classes will be [”cat”,”dog”,”pikachu”] instead.

Donny Hurley Logistic Regression 21 / 24



Programming

A new method it has is

1 model . p r e d i c t p r o b a ( X t e s t )

This one gives probability estimates for all the classes. Maybe you want to know how
confident the model is in its predictions - but be aware, models can be over confident!

The .predict I mentioned on the previous slide uses thresholds to make the decisions.

You may also want the probabilities for some performance metrics when trying to make
decisions about models.

Donny Hurley Logistic Regression 22 / 24



Transform Data
Sometimes the data we have is not in the best form for the model we are training, so
we need to change it. However

We don’t alter the entire data set
This would mean any future data being evaluated by the model would also first
have to be transformed
We build the transform into the model using make pipeline

1 from s k l e a r n . p i p e l i n e impor t mak e p i p e l i n e
2

3 model = mak e p i p e l i n e (
4 MinMaxScaler ( ) ,
5 L o g i s t i c R e g r e s s i o n ( )
6 )
7 model . f i t ( X t r a i n , y t r a i n )
8 model . s c o r e ( X te s t , y t e s t )

Any data that goes into this pipeline has the first step: the data is transformed, then
the transformed data goes into the LogisticRegression step. We build the
transformation into the model. We can chain more things together in a pipeline.

Donny Hurley Logistic Regression 23 / 24



Transform Data

The example I had on the previous slide was MinMaxScaler which is a very commonly
used transformation step, https://scikit-learn.org/stable/modules/
generated/sklearn.preprocessing.MinMaxScaler.html.

It makes all the data in the same range, by default the minimum value is changed to 0,
the maximum to 1 and everything else scaled accordingly.

Donny Hurley Logistic Regression 24 / 24